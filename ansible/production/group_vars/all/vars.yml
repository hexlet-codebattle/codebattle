cluster_region: "{{ vault_cluster_region }}"
cluster_name: "{{ vault_cluster_name }}"
cluster_node_name: "{{ vault_cluster_node_name }}"
cluster_node_size: "{{ vault_cluster_node_size }}"
postgres_db_node_size: "{{ vault_postgres_db_node_size }}"
postgres_version: "{{ vault_postgres_version }}"
postgres_db_cluster_name: "{{ vault_postgres_db_cluster_name }}"

codebattle_port: 4000
codebattle_github_id: "{{ vault_codebattle_github_id }}"
codebattle_github_secret: "{{ vault_codebattle_github_secret }}"
codebattle_discord_id: "{{ vault_codebattle_discord_id }}"
codebattle_discord_secret: "{{ vault_codebattle_discord_secret }}"
codebattle_db_username: "{{ vault_codebattle_db_username }}"
codebattle_db_name: "{{ vault_codebattle_db_name }}"
codebattle_secret_key_base: "{{ vault_codebattle_secret_key_base }}"
codebattle_live_view_salt: "{{ vault_codebattle_live_view_salt }}"
codebattle_onesignal_app_id: "{{ vault_codebattle_onesignal_app_id }}"
codebattle_onesignal_api_key: "{{ vault_codebattle_onesignal_api_key }}"
codebattle_firebase_api_key: "{{ vault_codebattle_firebase_api_key }}"
codebattle_firebase_messaging_sender_id: "{{ vault_codebattle_firebase_messaging_sender_id }}"
codebattle_rollbar_api_key: "{{ vault_codebattle_rollbar_api_key }}"
